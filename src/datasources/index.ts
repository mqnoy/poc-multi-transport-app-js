import { Sequelize } from 'sequelize';

export class DataSources {
  sequelize: Sequelize;
  init = async () => {
    // TODO: initialize sequlize
    // TOOD: initialize
  };
}

export const dataSources = new DataSources();
